<% if line_item == @current_item %>
<tr id="current_item">
<% else %>
<tr class="item">
<% end %>
	<td><%= line_item.quantity %> &times;</td>
	<td><%= line_item.product.title%></td>
	<td class="item_price"><%= line_item.total_price.to_s + " руб." %></td>
	<% unless @mail %>
	<td><%= button_to '-', decrement_line_item_path(line_item), method: :put, remote: true %></td>
	<td><%= button_to '+', increment_line_item_path(line_item), method: :put, remote: true %></td>
	<td><%= button_to 'Удалить', line_item, method: :delete, remote: true %></td>
	<% end %>
</tr>